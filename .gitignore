node_modules
dist
.DS_Store
.vite
*.log
.branches
.temp

# Env files with secrets. .env.example is committed as a setup reference;
# every real .env / .env.local stays local.
.env
.env.local
.env.*.local

# Override global config for migration SQL files
!supabase/migrations/*.sql

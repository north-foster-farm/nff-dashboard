import ComingSoon from "../components/ComingSoon.jsx";

// Stub Settings page. The real preferences UI (light/dark choice, auto-expand
// chore groups) lands in Batch 5 along with the user_preferences table.
export default function Settings() {
  return <ComingSoon featureName="User settings" />;
}
